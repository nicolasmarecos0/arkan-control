# PostgreSQL connection used by the app, the migrations and the seed.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/arkan_control_dev

# PostgreSQL connection used by `npm test`. Optional: when unset the test suite
# falls back to postgres://postgres:postgres@127.0.0.1:5432/arkan_control_test
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/arkan_control_test
